<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.ReceiptAs" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>

        <!-- Customize your theme here. -->
        <item name="colorControlNormal">@color/white</item>
        <item name="colorControlHighlight">@color/black</item>

        <item name="backgroundColor">@color/background_material_light</item>

        <item name="correctionItemColor">?attr/colorControlHighlight</item>
        <item name="correctionItemCombinedColor">@color/purple_500</item>
        <item name="correctionItemDeletedColor">@color/red</item>

        <item name="participantStrokeColor">@color/purple_500</item>
        <item name="participantFillColor">@color/purple_50</item>
        <item name="payerStrokeColor">@color/teal_500</item>
        <item name="payerFillColor">@color/teal_50</item>
    </style>

    <style name="Theme.ReceiptAs.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.ReceiptAs.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.ReceiptAs.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>