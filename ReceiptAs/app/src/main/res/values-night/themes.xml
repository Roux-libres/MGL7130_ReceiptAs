<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.ReceiptAs" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_200</item>
        <item name="colorPrimaryVariant">@color/purple_500</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>

        <!-- Customize your theme here. -->
        <item name="colorControlNormal">@color/black</item>
        <item name="colorControlHighlight">@color/white</item>

        <item name="backgroundColor">#FF101010</item>

        <item name="correctionItemColor">?attr/colorControlHighlight</item>
        <item name="correctionItemCombinedColor">@color/purple_200</item>
        <item name="correctionItemDeletedColor">@color/red</item>

        <item name="participantStrokeColor">@color/purple_50</item>
        <item name="participantFillColor">@color/purple_200</item>
        <item name="payerStrokeColor">@color/teal_50</item>
        <item name="payerFillColor">@color/teal_700</item>
    </style>

    <style name="Theme.ReceiptAs.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.ReceiptAs.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.ReceiptAs.PopupOverlay" parent="ThemeOverlay.AppCompat.Dark" />
</resources>